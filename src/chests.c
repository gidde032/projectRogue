#include <rogue.h>

/* Entity* createChest(Position chest_pos) {
    Entity* newChest = calloc(1, sizeof(Entity));
    
    newChest->pos.y = chest_pos.y;
    newChest->pos.x = chest_pos.x;
    newChest->ch = '^';
    newChest->color = COLOR_PAIR(VISIBLE_COLOR);
    return newChest;
} */